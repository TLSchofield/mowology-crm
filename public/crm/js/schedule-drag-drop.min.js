(function(){'use strict';var DRAG_THRESHOLD=6;var pressCard=null;var pressPointerId=null;var pressStartX=0;var pressStartY=0;var dragging=!1;var draggedCard=null;var originalDate=null;var originalRouteOrder=null;var cardClone=null;var cloneOffsetX=0;var cloneOffsetY=0;var dropIndicator=null;var currentColumn=null;var currentBefore=null;var currentIndex=0;var feedbackEl=document.getElementById('dragFeedback');var feedbackMsg=document.getElementById('dragMessage');var autoHideTimer=null;function initDragAndDrop(){var stopCards=document.querySelectorAll('.mw-stop-card');stopCards.forEach(function(card){card.addEventListener('pointerdown',handlePointerDown)})}
function handlePointerDown(e){if(e.button!==undefined&&e.button!==0)return;pressCard=this;pressPointerId=e.pointerId;pressStartX=e.clientX;pressStartY=e.clientY;dragging=!1;if(this.setPointerCapture){try{this.setPointerCapture(e.pointerId)}catch(err){}}
document.addEventListener('pointermove',handlePointerMove,{passive:!1});document.addEventListener('pointerup',handlePointerUp);document.addEventListener('pointercancel',handlePointerCancel)}
function handlePointerMove(e){if(!pressCard||e.pointerId!==pressPointerId)return;if(!dragging){var dx=e.clientX-pressStartX;var dy=e.clientY-pressStartY;if(Math.sqrt(dx*dx+dy*dy)<DRAG_THRESHOLD)return;startDrag(e)}
e.preventDefault();moveCloneTo(e.clientX,e.clientY);updateDropTarget(e.clientX,e.clientY)}
function handlePointerUp(e){if(!pressCard||e.pointerId!==pressPointerId)return;cleanupPointerListeners();if(dragging){finishDrag()}
resetPressState()}
function handlePointerCancel(e){if(!pressCard||e.pointerId!==pressPointerId)return;cleanupPointerListeners();if(dragging){cancelDrag()}
resetPressState()}
function cleanupPointerListeners(){document.removeEventListener('pointermove',handlePointerMove);document.removeEventListener('pointerup',handlePointerUp);document.removeEventListener('pointercancel',handlePointerCancel);if(pressCard&&pressCard.releasePointerCapture){try{pressCard.releasePointerCapture(pressPointerId)}catch(err){}}}
function resetPressState(){pressCard=null;pressPointerId=null;dragging=!1}
function startDrag(e){dragging=!0;draggedCard=pressCard;originalDate=draggedCard.dataset.stopDate||'';originalRouteOrder=parseInt(draggedCard.dataset.routeOrder,10)||0;var rect=draggedCard.getBoundingClientRect();cloneOffsetX=pressStartX-rect.left;cloneOffsetY=pressStartY-rect.top;cardClone=draggedCard.cloneNode(!0);cardClone.classList.add('mw-stop-card-clone');cardClone.classList.remove('dragging');cardClone.style.width=rect.width+'px';document.body.appendChild(cardClone);moveCloneTo(e.clientX,e.clientY);draggedCard.classList.add('dragging');currentColumn=resolveColumn(draggedCard);currentBefore=draggedCard.nextElementSibling;currentIndex=originalRouteOrder>0?originalRouteOrder-1:0}
function moveCloneTo(clientX,clientY){if(!cardClone)return;cardClone.style.left=(clientX-cloneOffsetX)+'px';cardClone.style.top=(clientY-cloneOffsetY)+'px'}
function updateDropTarget(clientX,clientY){var el=document.elementFromPoint(clientX,clientY);var column=resolveColumn(el);document.querySelectorAll('.mw-day-column.drag-over').forEach(function(col){if(col!==column)col.classList.remove('drag-over');});if(!column){currentColumn=null;removeDropIndicator();return}
column.classList.add('drag-over');currentColumn=column;var insertion=findInsertionPoint(column,clientY);currentBefore=insertion.before;currentIndex=insertion.index;showDropIndicatorAt(column,insertion.before)}
function findInsertionPoint(column,clientY){var cards=Array.prototype.slice.call(column.querySelectorAll('.mw-stop-card:not(.dragging)'));for(var i=0;i<cards.length;i++){var rect=cards[i].getBoundingClientRect();var midY=rect.top+rect.height/2;if(clientY<midY){return{index:i,before:cards[i]}}}
return{index:cards.length,before:null}}
function ensureDropIndicator(){if(!dropIndicator){dropIndicator=document.createElement('div');dropIndicator.className='mw-drop-indicator'}
return dropIndicator}
function showDropIndicatorAt(column,before){var indicator=ensureDropIndicator();if(before){column.insertBefore(indicator,before)}else{column.appendChild(indicator)}}
function removeDropIndicator(){if(dropIndicator&&dropIndicator.parentNode){dropIndicator.parentNode.removeChild(dropIndicator)}}
function finishDrag(){removeDropIndicator();document.querySelectorAll('.mw-day-column.drag-over').forEach(function(col){col.classList.remove('drag-over')});if(cardClone&&cardClone.parentNode)cardClone.parentNode.removeChild(cardClone);cardClone=null;var card=draggedCard;draggedCard=null;if(!currentColumn){card.classList.remove('dragging');return}
var newDate=currentColumn.dataset.date;if(!newDate){card.classList.remove('dragging');return}
var newRouteOrder=currentIndex+1;if(newDate===originalDate&&newRouteOrder===originalRouteOrder){card.classList.remove('dragging');showFeedback('Stop not moved (same position)','warning');return}
rescheduleStop(card,currentColumn,currentBefore,newDate,newRouteOrder)}
function cancelDrag(){removeDropIndicator();document.querySelectorAll('.mw-day-column.drag-over').forEach(function(col){col.classList.remove('drag-over')});if(cardClone&&cardClone.parentNode)cardClone.parentNode.removeChild(cardClone);cardClone=null;if(draggedCard)draggedCard.classList.remove('dragging');draggedCard=null}
function rescheduleStop(card,targetColumn,before,newDate,newRouteOrder){var stopId=parseInt(card.dataset.stopId,10);if(!newDate||!/^\d{4}-\d{2}-\d{2}$/.test(newDate)){card.classList.remove('dragging');showFeedback('Invalid date format: "'+newDate+'"','error');return}
showFeedback('Updating schedule...','loading');var payload={stop_id:stopId,new_date:newDate,new_route_order:newRouteOrder,csrf_token:window.MW_CSRF||''};fetch('/crm/api/reschedule-stop.php',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':window.MW_CSRF||''},body:JSON.stringify(payload)}).then(function(response){if(!response.ok){return response.json().catch(function(){return null}).then(function(data){throw new Error((data&&data.error)||'Server error ('+response.status+')')})}
return response.json()}).then(function(data){if(data&&data.warning){card.classList.remove('dragging');showCapacityWarning(card,targetColumn,before,newDate,newRouteOrder,data.message);return}
applyMove(card,targetColumn,before,newDate,newRouteOrder,data);showFeedback('Stop moved to '+formatFriendlyDate(newDate)+' (position '+newRouteOrder+')','success')}).catch(function(error){card.classList.remove('dragging');var msg=error.message||'Failed to reschedule stop';if(error instanceof TypeError&&error.message==='Failed to fetch'){msg='Connection error — check your internet and try again'}
showFeedback(msg,'error');console.error('Reschedule stop error:',error)})}
function applyMove(card,targetColumn,before,newDate,newRouteOrder,data){var fromRect=card.getBoundingClientRect();if(before){targetColumn.insertBefore(card,before)}else{targetColumn.appendChild(card)}
card.classList.remove('dragging');card.dataset.stopDate=newDate;card.dataset.routeOrder=String(newRouteOrder);var toRect=card.getBoundingClientRect();var dx=fromRect.left-toRect.left;var dy=fromRect.top-toRect.top;if((dx||dy)&&typeof card.animate==='function'){card.animate([{transform:'translate('+dx+'px,'+dy+'px)'},{transform:'none'}],{duration:200,easing:'cubic-bezier(0.32,0.72,0,1)'})}
patchDayCards((data&&data.day_cards)||{})}
function patchDayCards(dayCards){Object.keys(dayCards).forEach(function(date){var column=document.querySelector('.mw-day-column[data-date="'+date+'"]');if(!column)return;var existing=column.querySelector('.mw-dsc');if(existing){existing.outerHTML=dayCards[date]}else{column.insertAdjacentHTML('afterbegin',dayCards[date])}})}
function formatFriendlyDate(dateStr){var dateObj=new Date(dateStr+'T12:00:00');return dateObj.toLocaleDateString('en-US',{weekday:'short',month:'short',day:'numeric'})}
function resolveColumn(el){if(!el)return null;if(el.classList&&el.classList.contains('mw-day-column'))return el;return el.closest?el.closest('.mw-day-column'):null}
function showCapacityWarning(card,targetColumn,before,newDate,newRouteOrder,message){if(!feedbackEl||!feedbackMsg)return;clearTimeout(autoHideTimer);autoHideTimer=null;feedbackMsg.textContent=message||'Crew day is over capacity — continue?';feedbackEl.className='mw-drag-feedback warning';feedbackEl.style.display='block';feedbackEl.querySelectorAll('.mw-cap-confirm-btn, .mw-cap-cancel-btn').forEach(function(el){el.remove()});var cancelBtn=document.createElement('button');cancelBtn.type='button';cancelBtn.textContent='Cancel';cancelBtn.className='mw-cap-cancel-btn';cancelBtn.style.cssText='margin-left:8px;padding:2px 10px;font-size:11px;cursor:pointer;background:transparent;color:#fff;border:1px solid rgba(255,255,255,0.4);border-radius:4px;';feedbackEl.appendChild(cancelBtn);var btn=document.createElement('button');btn.type='button';btn.textContent='Continue anyway';btn.className='mw-cap-confirm-btn';btn.style.cssText='margin-left:6px;padding:2px 10px;font-size:11px;cursor:pointer;background:#e85d04;color:#fff;border:none;border-radius:4px;';feedbackEl.appendChild(btn);cancelBtn.addEventListener('click',function(){cancelBtn.remove();btn.remove();feedbackEl.style.display='none'});btn.addEventListener('click',function(){btn.remove();var stopId=parseInt(card.dataset.stopId,10);showFeedback('Moving stop...','loading');fetch('/crm/api/reschedule-stop.php',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':window.MW_CSRF||''},body:JSON.stringify({stop_id:stopId,new_date:newDate,new_route_order:newRouteOrder,force:1,csrf_token:window.MW_CSRF||''})}).then(function(r){return r.json()}).then(function(data){if(data&&data.success){applyMove(card,targetColumn,before,newDate,newRouteOrder,data);showFeedback('Stop moved (over capacity)','success')}else{showFeedback((data&&data.error)||'Move failed','error')}}).catch(function(){showFeedback('Connection error','error')})})}
function showFeedback(message,type){if(!feedbackEl||!feedbackMsg)return;clearTimeout(autoHideTimer);autoHideTimer=null;feedbackEl.querySelectorAll('.mw-cap-confirm-btn, .mw-cap-cancel-btn').forEach(function(el){el.remove()});type=type||'info';feedbackMsg.textContent=message;feedbackEl.className='mw-drag-feedback';if(type==='error'){feedbackEl.classList.add('error')}else if(type==='success'){feedbackEl.classList.add('success')}else if(type==='warning'){feedbackEl.classList.add('warning')}
feedbackEl.style.display='block';var timeout;switch(type){case 'error':timeout=10000;break;case 'loading':timeout=30000;break;case 'success':timeout=3000;break;case 'warning':timeout=6000;break;default:timeout=3000}
autoHideTimer=setTimeout(function(){feedbackEl.style.display='none';autoHideTimer=null},timeout)}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',initDragAndDrop)}else{initDragAndDrop()}
window.reinitScheduleDragDrop=initDragAndDrop})()
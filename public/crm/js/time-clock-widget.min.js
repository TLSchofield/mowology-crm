(function(){'use strict';var widget=document.getElementById('clockWidget');if(!widget)return;var timerInterval=null;var clockInTime=null;var deviceType='personal';var trackingEnabled=!1;var gpsWatchId=null;var trackingInterval=null;var latestPosition=null;var TRACKING_INTERVAL_MS=30000;var GPS_INTERVAL_STANDARD=30000;var GPS_INTERVAL_HEIGHTENED=10000;var hasActiveJobTimer=!1;var autoArrivalEnabled=!1;var lastAutoStartVisitId=null;var wakeLock=null;function requestWakeLock(){if(!('wakeLock' in navigator))return;if(wakeLock)return;navigator.wakeLock.request('screen').then(function(lock){wakeLock=lock;wakeLock.addEventListener('release',function(){wakeLock=null})}).catch(function(){})}
function releaseWakeLock(){if(wakeLock){wakeLock.release().catch(function(){});wakeLock=null}}
updateTrackingDot('unknown','Checking GPS...');fetchStatus();function fetchStatus(){fetch('/crm/api/time-clock.php?action=status',{credentials:'same-origin'}).then(function(r){if(!r.ok)throw new Error('HTTP '+r.status);return r.json()}).then(function(data){if(!data.success){renderClockedOut();return}
trackingEnabled=!!data.location_tracking_enabled;deviceType=data.device_type||'personal';hasActiveJobTimer=!!(data.active_job);if(data.gps_interval_standard_ms){GPS_INTERVAL_STANDARD=data.gps_interval_standard_ms;TRACKING_INTERVAL_MS=GPS_INTERVAL_STANDARD}
if(data.gps_interval_heightened_ms){GPS_INTERVAL_HEIGHTENED=data.gps_interval_heightened_ms}
if(data.clocked_in){clockInTime=new Date(data.clock_in.replace(' ','T'));renderClockedIn(data.elapsed_seconds,data.active_job)}else{renderClockedOut()}
autoArrivalEnabled=!!data.auto_arrival_enabled;if(trackingEnabled){if(deviceType==='truck'){startTracking()}else if(hasActiveJobTimer){startTracking()}else{probeGPSStatus()}}else{probeGPSStatus()}
if(autoArrivalEnabled&&!hasActiveJobTimer){runOneShotProximityCheck()}}).catch(function(err){console.warn('Time clock API error:',err);updateTrackingDot('error','Unable to check status');renderClockedOut()})}
var gpsState='unknown';var trackingWrapper=document.getElementById('trackingDotWrapper');if(trackingWrapper){trackingWrapper.addEventListener('click',onTrackingDotTap)}
var mobileCrosshair=document.getElementById('mobileTrackingDot');if(mobileCrosshair){mobileCrosshair.addEventListener('click',function(e){if(gpsState!=='active'){e.preventDefault();e.stopPropagation();onTrackingDotTap()}})}
function updateTrackingDot(state,detail){gpsState=state;var dotClass,titleText;if(state==='active'){dotClass='mw-tracking-dot mw-tracking-dot-on';titleText='GPS active'+(detail?' — '+detail:'')}else if(state==='error'){dotClass='mw-tracking-dot mw-tracking-dot-off';titleText=(detail||'GPS off')+' — tap to enable'}else{dotClass='mw-tracking-dot mw-tracking-dot-loading';titleText=detail||'Checking GPS...'}
var dot=document.getElementById('trackingDot');var wrapper=document.getElementById('trackingDotWrapper');if(dot)dot.className=dotClass;if(wrapper)wrapper.title=titleText;var mBtn=document.getElementById('mobileTrackingDot');if(mBtn){mBtn.classList.remove('mw-mc-locate-on','mw-mc-locate-off','mw-mc-locate-loading');if(state==='active'){mBtn.classList.add('mw-mc-locate-on')}else if(state==='error'){mBtn.classList.add('mw-mc-locate-off')}else{mBtn.classList.add('mw-mc-locate-loading')}
mBtn.title=titleText}}
function onTrackingDotTap(){if(gpsState==='active'){if(latestPosition){showToast('GPS active — accuracy: '+Math.round(latestPosition.accuracy)+'m','success')}else{showToast('GPS active','success')}
return}
if(!navigator.geolocation){showToast('Location not supported on this browser','error');return}
updateTrackingDot('unknown','Requesting GPS...');showToast('Requesting location access...','info');navigator.geolocation.getCurrentPosition(function(pos){updateTrackingDot('active','accuracy: '+Math.round(pos.coords.accuracy)+'m');showToast('GPS enabled — location active','success');if(trackingEnabled&&clockInTime!==null){stopTracking();startTracking()}},function(err){if(err.code===1){updateTrackingDot('error','Permission denied');showToast('Location denied — open browser settings to allow location for this site','error')}else if(err.code===2){updateTrackingDot('error','GPS unavailable');showToast('GPS unavailable — enable Location Services in your phone settings','error')}else{updateTrackingDot('error','GPS timed out');showToast('GPS timed out — try again or check phone settings','error')}},{enableHighAccuracy:!0,timeout:10000,maximumAge:0})}
function probeGPSStatus(){if(!navigator.geolocation){updateTrackingDot('error','Not supported');return}
if(navigator.permissions&&navigator.permissions.query){navigator.permissions.query({name:'geolocation'}).then(function(result){if(result.state==='granted'){updateTrackingDot('active','Permission granted')}else if(result.state==='denied'){updateTrackingDot('error','Permission denied')}else{updateTrackingDot('error','Location not enabled')}}).catch(function(){quickGPSProbe()})}else{quickGPSProbe()}}
function quickGPSProbe(){navigator.geolocation.getCurrentPosition(function(pos){updateTrackingDot('active','accuracy: '+Math.round(pos.coords.accuracy)+'m')},function(err){if(err.code===1){updateTrackingDot('error','Permission denied')}else{updateTrackingDot('error','GPS unavailable')}},{enableHighAccuracy:!1,timeout:5000,maximumAge:300000})}
var SVG_PLAY='<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><polygon points="10 8 16 12 10 16 10 8"></polygon></svg>';var SVG_STOP='<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect></svg>';var SVG_CLOCK='<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>';var SVG_NAV='<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="3 11 22 2 13 21 11 13 3 11"></polygon></svg>';function renderClockedOut(){stopTimer();if(deviceType!=='truck'){stopTracking()}
widget.innerHTML='<button class="mw-clock-btn mw-clock-in" id="btnClockIn" title="Clock In">'+SVG_PLAY+'<span class="mw-clock-label">Clock In</span>'+'</button>';document.getElementById('btnClockIn').addEventListener('click',doClockIn)}
function renderClockedIn(elapsedSeconds,activeJob){var html='<div class="mw-clock-active">'+'<span class="mw-clock-icon-pulse">'+SVG_CLOCK+'</span>'+'<span class="mw-clock-timer" id="clockTimer">'+formatSeconds(elapsedSeconds)+'</span>';if(activeJob){html+='<span class="mw-clock-job-badge" title="'+escapeHtml(activeJob.job_title||'')+'">'+escapeHtml(activeJob.job_number||'')+'</span>'}
if(trackingEnabled){html+='<span class="mw-tracking-indicator" id="trackingIndicator" title="GPS tracking active">'+SVG_NAV+'</span>'}
html+='<button class="mw-clock-btn mw-clock-out" id="btnClockOut" title="Clock Out">'+SVG_STOP+'<span class="mw-clock-label">Out</span>'+'</button>'+'</div>';widget.innerHTML=html;document.getElementById('btnClockOut').addEventListener('click',doClockOut);startTimer(elapsedSeconds)}
function renderDisabled(){renderClockedOut()}
function startTimer(startSeconds){stopTimer();var seconds=startSeconds;var timerEl=document.getElementById('clockTimer');timerInterval=setInterval(function(){seconds++;if(timerEl)timerEl.textContent=formatSeconds(seconds);},1000)}
function stopTimer(){if(timerInterval){clearInterval(timerInterval);timerInterval=null}}
var gpsErrorCount=0;var gpsErrorToastShown=!1;function startTracking(){gpsErrorCount=0;gpsErrorToastShown=!1;updateTrackingDot('unknown','Acquiring GPS...');requestWakeLock();if(window.MwNative&&window.MwNative.geo){window.MwNative.geo.startBackgroundTracking(function(pos,error){if(error){gpsErrorCount++;updateTrackingDot('error',error.code||'GPS error');if(!gpsErrorToastShown){gpsErrorToastShown=!0;showToast('GPS error: '+(error.message||'Unknown'),'error')}
return}
gpsErrorCount=0;latestPosition=pos;updateTrackingDot('active','accuracy: '+Math.round(pos.accuracy)+'m')})}
if(gpsWatchId!==null){if(!trackingInterval){sendPosition();trackingInterval=setInterval(sendPosition,TRACKING_INTERVAL_MS)}
return}
if(!navigator.geolocation){if(!window.MwNative){showToast('Location not supported on this browser','error');updateTrackingDot('error','Not supported')}
if(!trackingInterval){sendPosition();trackingInterval=setInterval(sendPosition,TRACKING_INTERVAL_MS)}
return}
gpsWatchId=navigator.geolocation.watchPosition(function(pos){gpsErrorCount=0;latestPosition={lat:pos.coords.latitude,lng:pos.coords.longitude,accuracy:pos.coords.accuracy,speed:pos.coords.speed,heading:pos.coords.heading};updateTrackingDot('active','accuracy: '+Math.round(pos.coords.accuracy)+'m')},function(err){gpsErrorCount++;if(err.code===1){updateTrackingDot('error','Permission denied')}else if(err.code===2){updateTrackingDot('error','GPS unavailable')}else{updateTrackingDot('error','GPS timed out')}
if(!gpsErrorToastShown){gpsErrorToastShown=!0;if(err.code===1){showToast('Location permission denied — tap the signal icon to retry','error')}else if(err.code===2){showToast('GPS unavailable — enable Location in phone settings','error')}else{showToast('GPS timed out — trying again...','error')}}},{enableHighAccuracy:!0,timeout:15000,maximumAge:0});sendPosition();trackingInterval=setInterval(sendPosition,TRACKING_INTERVAL_MS)}
function stopTracking(){releaseWakeLock();if(window.MwNative&&window.MwNative.geo){window.MwNative.geo.stopBackgroundTracking()}
if(gpsWatchId!==null){navigator.geolocation.clearWatch(gpsWatchId);gpsWatchId=null}
if(trackingInterval){clearInterval(trackingInterval);trackingInterval=null}
latestPosition=null;if(!clockInTime){updateTrackingDot('unknown','Not clocked in')}}
var GPS_QUEUE_KEY='mw-gps-queue';var GPS_QUEUE_MAX=500;var noFixCount=0;function sendPosition(){if(!latestPosition){noFixCount++;if(noFixCount>=2&&navigator.geolocation){console.warn('[MwTracking] No GPS fix after '+noFixCount+' intervals — trying one-shot fallback');navigator.geolocation.getCurrentPosition(function(pos){latestPosition={lat:pos.coords.latitude,lng:pos.coords.longitude,accuracy:pos.coords.accuracy,speed:pos.coords.speed,heading:pos.coords.heading};updateTrackingDot('active','accuracy: '+Math.round(pos.coords.accuracy)+'m (fallback)');doSendPosition()},function(){console.warn('[MwTracking] One-shot fallback also failed')},{enableHighAccuracy:!0,timeout:10000,maximumAge:30000})}else{console.warn('[MwTracking] No GPS fix yet, skipping ping')}
return}
noFixCount=0;doSendPosition()}
function doSendPosition(){if(!latestPosition)return;var isOffline=(window.MwNative&&!window.MwNative.network.isOnline)||(!window.MwNative&&typeof navigator.onLine!=='undefined'&&!navigator.onLine);if(isOffline){queuePosition(latestPosition);console.log('[MwTracking] Offline — queued GPS ping');return}
flushQueue();fetch('/crm/api/crew-location.php',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({lat:latestPosition.lat,lng:latestPosition.lng,accuracy:latestPosition.accuracy,speed:latestPosition.speed,heading:latestPosition.heading})}).then(function(r){if(r.status===401){stopTracking();showSessionExpiredBanner();return null}
return r.json()}).then(function(data){if(!data)return;if(data.success&&!data.skipped){console.log('[MwTracking] Position sent OK')}
if(data.error==='Not clocked in'||data.error==='Tracking not enabled'){stopTracking()}
if(data.auto_started){handleServerAutoStart(data.auto_started)}}).catch(function(err){console.warn('[MwTracking] Send failed, queueing:',err);queuePosition(latestPosition)})}
var _sessionExpiredBannerShown=!1;function showSessionExpiredBanner(){if(_sessionExpiredBannerShown)return;_sessionExpiredBannerShown=!0;stopTracking();var banner=document.createElement('div');banner.id='mw-session-expired-banner';banner.style.cssText='position:fixed;top:0;left:0;right:0;z-index:99999;background:#c0392b;color:#fff;padding:12px 16px;font-size:14px;font-weight:600;text-align:center;box-shadow:0 2px 8px rgba(0,0,0,.4)';banner.innerHTML='Your session has expired — <a href="/loginAuth/login.php" style="color:#fff;text-decoration:underline;">tap here to log back in</a>. Your GPS data is saved and will sync automatically.';document.body.prepend(banner);if(window.MwNative&&window.MwNative.notifications){window.MwNative.notifications.notify('Session Expired','Tap to log back in to Mowology CRM',9001)}
console.warn('[MwTracking] Session expired — tracking stopped, banner shown')}
function queuePosition(pos){try{var queue=JSON.parse(localStorage.getItem(GPS_QUEUE_KEY)||'[]');if(queue.length>=GPS_QUEUE_MAX)queue.shift();queue.push({lat:pos.lat,lng:pos.lng,accuracy:pos.accuracy,speed:pos.speed,heading:pos.heading,queued_at:new Date().toISOString()});localStorage.setItem(GPS_QUEUE_KEY,JSON.stringify(queue))}catch(e){console.warn('[MwTracking] Queue write failed:',e)}}
function flushQueue(){var queue;try{queue=JSON.parse(localStorage.getItem(GPS_QUEUE_KEY)||'[]')}catch(e){return}
if(queue.length===0)return;localStorage.removeItem(GPS_QUEUE_KEY);console.log('[MwTracking] Flushing '+queue.length+' queued GPS pings');queue.forEach(function(pos,i){setTimeout(function(){fetch('/crm/api/crew-location.php',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(pos)}).then(function(r){if(r.status===401){queuePosition(pos);showSessionExpiredBanner()}}).catch(function(){queuePosition(pos)})},i*200)})}
function doClockIn(){var btn=document.getElementById('btnClockIn');if(btn)btn.disabled=!0;getGPS(function(lat,lng){fetch('/crm/api/time-clock.php',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'clock_in',lat:lat,lng:lng})}).then(function(r){return r.json()}).then(function(data){if(data.success){clockInTime=new Date();fetchStatus();showToast('Clocked in at '+formatTime(new Date()),'success')}else{showToast(data.error||'Clock in failed','error');renderClockedOut()}}).catch(function(){showToast('Network error','error');if(btn)btn.disabled=!1})})}
function doClockOut(){var btn=document.getElementById('btnClockOut');if(!btn)return;if(btn.dataset.confirming==='1'){btn.dataset.confirming='0';btn.innerHTML=SVG_STOP+'<span class="mw-clock-label">Out</span>'}else{btn.dataset.confirming='1';btn.innerHTML=SVG_STOP+'<span class="mw-clock-label">Confirm?</span>';setTimeout(function(){if(btn&&btn.dataset.confirming==='1'){btn.dataset.confirming='0';btn.innerHTML=SVG_STOP+'<span class="mw-clock-label">Out</span>'}},3000);return}
btn.disabled=!0;if(deviceType!=='truck'){stopTracking()}
getGPS(function(lat,lng){fetch('/crm/api/time-clock.php',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'clock_out',lat:lat,lng:lng})}).then(function(r){return r.json()}).then(function(data){if(data.success){renderClockedOut();showToast('Clocked out — Total: '+data.total_formatted,'success')}else{showToast(data.error||'Clock out failed','error');if(btn)btn.disabled=!1}}).catch(function(){showToast('Network error','error');if(btn)btn.disabled=!1})})}
function getGPS(callback){if(window.MwNative&&window.MwNative.geo){var done=!1;var timer=setTimeout(function(){if(!done){done=!0;callback(null,null)}},4000);window.MwNative.geo.getCurrentPosition().then(function(pos){if(!done){done=!0;clearTimeout(timer);callback(pos.lat,pos.lng)}}).catch(function(){if(!done){done=!0;clearTimeout(timer);callback(null,null)}});return}
if(navigator.geolocation){navigator.geolocation.getCurrentPosition(function(pos){callback(pos.coords.latitude,pos.coords.longitude)},function(){callback(null,null)},{timeout:5000,maximumAge:60000})}else{callback(null,null)}}
function formatSeconds(totalSec){if(!totalSec||totalSec<0)totalSec=0;var h=Math.floor(totalSec/3600);var m=Math.floor((totalSec%3600)/60);var s=totalSec%60;return pad(h)+':'+pad(m)+':'+pad(s)}
function pad(n){return n<10?'0'+n:''+n}
function formatTime(d){return pad(d.getHours())+':'+pad(d.getMinutes())}
function escapeHtml(str){var div=document.createElement('div');div.appendChild(document.createTextNode(str));return div.innerHTML}
function activateFeather(){if(typeof feather!=='undefined')feather.replace();}
function showToast(message,type){var toast=document.createElement('div');toast.className='mw-clock-toast mw-clock-toast-'+(type||'info');toast.textContent=message;document.body.appendChild(toast);toast.offsetHeight;toast.classList.add('mw-clock-toast-visible');setTimeout(function(){toast.classList.remove('mw-clock-toast-visible');setTimeout(function(){toast.remove()},300)},3000)}
function handleServerAutoStart(info){console.log('[MwTracking] Server auto-started visit '+info.visit_id+' ('+info.job_title+') at '+info.distance_meters+'m');lastAutoStartVisitId=info.visit_id;hasActiveJobTimer=!0;if(info.clock_in_created){clockInTime=new Date()}
fetchStatus();showToast('Auto-started: '+(info.job_title||info.job_number||'Job')+' ('+info.distance_meters+'m away)','success');document.dispatchEvent(new CustomEvent('mw-proximity-auto-start',{detail:info}))}
function runOneShotProximityCheck(){if(!navigator.geolocation)return;navigator.geolocation.getCurrentPosition(function(pos){var lat=pos.coords.latitude;var lng=pos.coords.longitude;var accuracy=pos.coords.accuracy;console.log('[MwTracking] One-shot proximity check: lat='+lat+', lng='+lng+', accuracy='+Math.round(accuracy)+'m');fetch('/crm/api/crew-location.php',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({lat:lat,lng:lng,accuracy:accuracy,proximity_check:!0})}).then(function(r){return r.json()}).then(function(data){if(data.auto_started){handleServerAutoStart(data.auto_started)}}).catch(function(err){console.warn('[MwTracking] One-shot proximity check failed:',err)})},function(){},{enableHighAccuracy:!0,timeout:8000,maximumAge:30000})}
document.addEventListener('visibilitychange',function(){if(window.MwNative)return;if(document.visibilityState!=='visible'||!trackingEnabled)return;if(deviceType==='truck'||hasActiveJobTimer){stopTracking();startTracking()}});window.addEventListener('pageshow',function(event){if(window.MwNative)return;if(!event.persisted||!trackingEnabled)return;if(deviceType==='truck'||hasActiveJobTimer){stopTracking();startTracking()}});if(window.MwNative&&window.MwNative.network){window.MwNative.network.onStatusChange(function(isOnline){if(isOnline)flushQueue();})}else{window.addEventListener('online',function(){flushQueue()})}
window.MwTimeClock={fetchStatus:fetchStatus,isActive:function(){return clockInTime!==null},isTracking:function(){return gpsWatchId!==null||(window.MwNative&&window.MwNative.geo&&window.MwNative.geo.watchId!==null)},getDeviceType:function(){return deviceType},restartTracking:function(){if(trackingEnabled){stopTracking();startTracking()}},notifyJobTimerStarted:function(){hasActiveJobTimer=!0;if(trackingEnabled){startTracking()}},notifyJobTimerStopped:function(){hasActiveJobTimer=!1;if(deviceType==='personal'){stopTracking()}},setTrackingInterval:function(mode){if(mode==='heightened'){TRACKING_INTERVAL_MS=GPS_INTERVAL_HEIGHTENED}else if(mode==='standard'){TRACKING_INTERVAL_MS=GPS_INTERVAL_STANDARD}else if(typeof mode==='number'&&mode>0){TRACKING_INTERVAL_MS=mode}else{TRACKING_INTERVAL_MS=GPS_INTERVAL_STANDARD}
if(trackingInterval){clearInterval(trackingInterval);trackingInterval=setInterval(sendPosition,TRACKING_INTERVAL_MS)}}}})()